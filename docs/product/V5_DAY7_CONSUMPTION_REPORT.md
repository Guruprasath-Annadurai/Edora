# EDORA V5 — DAY 7 PRODUCT CONSUMPTION REPORT

Starting SHA `ea89c7aed1bca041e8baad943d879331b98e54ce` (fast-forward, worktree clean) · final SHA: `git log -1` · Production changes: NONE · Day 8 not started.

**Hook API** — `useTodayPlan(): { items: TodayPlanItem[]; isLoading; isFallback; error: Error|null; refetch }` (`src/hooks/useTodayPlan.ts`); pure logic in `src/lib/todayPlan.ts`.
**RPC handling** — exactly one `supabase.rpc('get_today_plan')` (no args, no Edge Function, no AI, no client ranking, no profile/table reads). Server order preserved; max 3. 8 s timeout. Result validated inside the query function, so the cache only ever holds valid rows.
**Validation** — a row is dropped unless: kind ∈ {review_due, fix_weak, diagnostic, learn_next, exam_push}; target_path ∈ {/spaced-review, /practice, /pyq-bank} **and agrees with the kind** (review_due↔/spaced-review, exam_push↔/pyq-bank, others↔/practice); priority integer 1–5; duration positive integer ≤ 240; target_params a plain object; non-empty title; subject/chapter/topic string|null; reason object. Non-array payloads yield [].
**Flag filtering** — client-only, returns a new array, cache untouched: `pyq_enabled=false` drops `/pyq-bank` items; if everything is filtered (or nothing valid/RPC failed/timed out/empty) the fallback is used. Flag changes never refetch.
**Fallback** — deterministic, not personalised, no AI: Practice `/practice`, then Ask Novo `/chat` only if `novo_enabled`. `isFallback=true`, `error` carries the failure. Signed-out: no RPC, fallback.
**Cache** — TanStack Query key `['today-plan', userId]`, staleTime 5 min, no polling, no refetch on focus/reconnect, `retry:false` (fail fast to fallback), `networkMode:'always'` (offline fails into fallback instead of pausing). Shared by all consumers; explicit `refetch()` = one RPC.
**Navigation helper** — `planItemDestination(item)` → `{pathname, search, to}`. Pathname comes from the kind, never the row. Per-kind param whitelist with type checks (review_due: due_count · fix_weak: mode=weak_topic, subject, subtopic · diagnostic: mode=diagnostic, questions_count 1–20 · learn_next: mode=learn_next, class 1–14, subject, chapter_id (UUID), chapter · exam_push: exam, days_remaining 0–365). Text ≤120 chars, no control characters; built with `URLSearchParams` (values encoded as data). Unknown keys/wrong types dropped. Practice-session interpretation of the params is NOT implemented (Day 8/12).
**Presentation boundary** — `kind` is the semantic key for future en/hi copy; server `title` retained as fallback/debug only. No Home UI touched.
**Tests** — `todayPlan.test.ts` + `useTodayPlan.test.tsx`: 46 new (RPC success/order/max-3; failure, network exception, timeout, empty → fallback with no AI; malformed rows; all flag cases incl. no-mutation and no-refetch; rerender/multi-consumer/remount/refetch/no-polling cache; zero `functions.invoke`/`/functions/v1`; 5 kinds' whitelisted navigation and injection cases). Gates: type-check pass · lint 0 errors (7 old warnings) · `npm test` 43 files / 353 tests (was 41 / 307) · build OK · schema guard PASSED.
**Known limitations** — nothing consumes the hook yet (Home is Day 8); `/practice` does not interpret the params; `get_today_plan` was not executed against a database here (backend is Antigravity's, verified by its own suite); the fallback is intentionally generic.
