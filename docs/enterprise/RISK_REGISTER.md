# Edora — Risk Register

**Status:** Phase 0 baseline. Populated from verified repository/tooling state and the existing `enterprise-remediation-tracker.md` audit history — not from assumption. New risks will be appended as later phases surface them; none will be silently removed without a documented resolution.

**Owner of this register:** Guruprasath Annadurai (founder — see `OWNERSHIP_MATRIX.md`).

| ID | Risk | Likelihood | Impact | Severity | Mitigation | Owner | Review date | Trigger | Fallback | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| RISK-001 | Single-person bus factor across every critical system (security, backend, AI, mobile, support, incident command) | High (certain today) | Critical | **Critical** | Documented explicitly rather than hidden behind fictional role assignments; prioritize hiring/contracting a second technical reviewer before scaling past closed beta | Guruprasath Annadurai | Every phase start | Founder unavailable for >48h during an incident | No fallback currently exists — this is the risk itself | ACCEPTED TEMPORARY RISK |
| RISK-002 | Supabase org on Free tier — no point-in-time recovery, only a daily snapshot with unproven restore | Medium (any bad migration/accidental delete) | Critical (permanent data loss for the gap since last snapshot) | **Critical** | Daily `db-backup-export` cron exists (§5.3); RPO ~24h. Upgrading to Pro is a billing decision outside engineering scope | Guruprasath Annadurai | Before any destructive migration; before Phase 2 exit | Any `DELETE`/`DROP` without a tested rollback path | Manual snapshot restore via `db-backup-restore` (untested as a full drill as of Phase 0) | ACCEPTED TEMPORARY RISK |
| RISK-003 | Backup restore process has never been executed as a full drill (only a scoped single-table restore during an incident recovery) | Medium | High | **High** | **PARTIALLY RESOLVED.** `docs/operations/DISASTER_RECOVERY_TEST_REPORT.md`: a full-scale (174-table) dry-run restore against production matched `backup_rows` to `live_rows_before` exactly, zero failures — real, rigorous, first time run at full scale. The write-path restore remains untested: **re-confirmed 2026-08-08** via a live `create_branch` attempt (not from memory) — `PaymentRequiredException: Branching is supported only on the Pro plan or above`. No isolated environment exists on the current (Free) plan, and a full write-restore against production itself carries real risk this pass has no standing to accept unilaterally. | Guruprasath Annadurai | Phase 2 exit | Attempted use of `db-backup-restore` in a real incident | Dry-run reconciliation path fully verified at scale; write-path needs either a Pro-plan upgrade (for isolated branching) or an explicit founder-approved maintenance-window production drill | PARTIALLY COMPLETE |
| RISK-004 | ~39 of 41 Supabase-linter-flagged database functions were sampled by risk, not individually reviewed | Medium | High (unknown privilege-escalation paths may exist, similar to the `increment_xp` bug already found and fixed) | **High** | **RESOLVED Phase 1.1: all 42 currently-flagged functions individually reviewed, zero sampled — see `docs/security/DATABASE_FUNCTION_AUDIT.md`. One live finding (RISK-019) found and fixed; one (RISK-020) accepted-low.** | Guruprasath Annadurai | Phase 1 exit | Any new linter finding or user-reported anomaly | N/A — resolved | VERIFIED COMPLETE |
| RISK-005 | No systematic IDOR / tampered-JWT / session-lifecycle test suite exists | Medium | High | **High** | **PARTIALLY RESOLVED, this row was stale** (said NOT STARTED despite Phase 1.4 already covering 7 categories — corrected here). `supabase/tests/database/security_attack_tests.sql` now covers 8 of 19 mandate categories, 14 assertions. **Extended 2026-08-08**: found and fixed a new real, directly-exploitable vulnerability (`institution_members` self-insert let any user set `role='admin'` on themselves, bypassing `join_institution()`'s intended `role='student'`-only self-join — confirmed live pre-fix, fixed, re-confirmed live post-fix against the actually-applied policy, zero regression). Applied to production; staging sync blocked on the project being auto-paused (free-tier inactivity) and `restore_project` hitting the org's 2-active-free-project cap — needs founder action via the Supabase dashboard. CI wiring added (`security-attack-tests` job, Supabase's own `setup-cli`/`start`/`test db` pattern) but not yet observed passing — local Docker was unresponsive this session, so it could not be dry-run first. A JWT-role-claim-tampering test attempt was found to be a false positive of the `set_config`-based technique itself (bypasses PostgREST's signature verification), not a real finding — documented as a methodology limitation in `docs/security/ATTACK_TESTS.md` rather than reported as a vulnerability. | Guruprasath Annadurai | Phase 1 exit | Any authorization-bypass bug report | 8/19 categories covered with real, individually-verified assertions; CI wiring exists but unobserved; 11 categories remain (tampered-JWT specifically requires HTTP-level testing this file's technique cannot provide) | PARTIALLY COMPLETE |
| RISK-006 | No central AI gateway — every feature calls Gemini/Groq/NVIDIA directly with no cost ceiling, no kill switch, no quota enforcement | Medium (cost spike); Low-Medium (abuse) | High (unbounded AI spend, no way to degrade gracefully on provider outage) | **High** | **PARTIALLY RESOLVED, corrected 2026-08-08**: Phase 7 built the gateway (`supabase/functions/_shared/aiGateway.ts`, kill switch + daily cost ceiling, fail-closed, full request log) but — despite the original tracker claiming it was "applied and verified on edora-staging" — a production-hardening pass found it had **never been deployed to production at all**, and even on staging `service_role` had zero table-level grants on either gateway table (RLS policies existed but the underlying GRANT didn't, so every gateway check had been silently failing closed since Phase 7 shipped). Both bugs fixed via `supabase/migrations/20260808_ai_gateway_production_deploy_and_grant_fix.sql`, applied to both projects. `gemini-vision` (6 actions, single shared call path) is now also migrated alongside `ai-question-gen`, deployed to **both** staging and production, and re-verified live end-to-end on staging (real Gemini API call reached, real response logged) — the first genuine full-pipeline proof, since the earlier "verified" claim only checked a config read that bypassed grants. `admin-console`'s kill-switch actions were also missing from production entirely; deployed. See `docs/enterprise/AI_GATEWAY_MIGRATION.md`'s "Production deploy correction" section for the full writeup. **Still open**: 37 of ~39 functions remain direct, including the highest-traffic surface (`gemini-chat`) — unchanged in scope, just now on an infrastructure that actually reaches production. | Guruprasath Annadurai | Phase 7 exit | Any unexplained AI cost spike or provider outage during peak usage | For the 2 migrated functions: kill switch + cost ceiling, now live on production. For the other 37: none | PARTIALLY COMPLETE |
| RISK-007 | No golden evaluation sets for AI-generated academic content — correctness is not measured, only spot-checked historically | Medium | High (wrong scoring/explanations reaching students preparing for high-stakes exams) | **High** | Phase 8 mandates golden eval sets per subject/exam | Guruprasath Annadurai | Phase 8 exit | Any user-reported wrong-answer pattern exceeding normal report volume | `report-wrong-answer` feature exists and is wired across 9+ AI surfaces — reactive only, not preventive | PARTIALLY COMPLETE (reactive path only) |
| RISK-008 | Mock exam submission is not proven idempotent; no exam-config versioning; interruption recovery is unverified | Low-Medium | High (a student's real exam attempt could be lost or scored against changed rules) | **High** | **RESOLVED Phase 5**: migration `20260814_mock_exam_integrity.sql` adds `attempt_key` (client-minted UUID at exam start, unique-indexed) + `config_version` to `mock_test_attempts`. `submitExam()` reuses the same key on retry/resume and falls back to the already-recorded row on a unique-violation instead of erroring or duplicating (also skips re-running `upsert_topic_stat`/`sr_cards` side effects on that fallback path). `src/lib/mockExamRecovery.ts` persists a localStorage snapshot (6h TTL, per-user-scoped, discarded on a config-version mismatch) on every answer/state change during `phase === 'exam'`; `MockTestPage.tsx` offers a Resume/Discard prompt on next mount. Verified live end-to-end against `edora-staging`: started a real CAT attempt, answered Q1, hard-reloaded (simulating a crash), confirmed the resume banner appeared with the correct answered-count, resumed, and confirmed the timer continued from its saved value (not reset) and the prior answer was intact. 10 unit tests added to `mockScoring.test.ts` (2 new), 8 new tests for `mockExamRecovery.ts`. Idempotency also verified directly against the DB: a rolled-back transaction inserting the same `attempt_key` twice confirmed the unique index rejects it with `23505` and leaves no duplicate row. | Guruprasath Annadurai | Phase 5 exit | Any user report of a lost/duplicated mock attempt | N/A — resolved | RESOLVED |
| RISK-009 | Offline conflict resolution is undefined/unproven for flashcards, quiz progress, roadmap changes, mock answers | Low-Medium | Medium-High (silent progress loss on flaky networks, common on Indian mobile networks) | **High** | **RESOLVED Phase 6**: `docs/operations/OFFLINE_CONFLICT_RESOLUTION.md` documents the real conflict rule per action type. Found and fixed a genuine bug: `SyncQueue.flush()` (`src/lib/syncQueue.ts`) only persisted the updated queue once, after the whole batch — an interruption mid-flush (network drops again, app backgrounded/killed) left already-succeeded entries still queued, replaying them on the next flush. This was the inverse of the risk as originally phrased: not progress *lost*, but progress *duplicated* (double XP, duplicate quiz_session/quiz_answer rows). Fixed to persist after every entry. Also switched `xp_grant` from a non-atomic read-then-write to the existing atomic `increment_xp` RPC (which also fixes a latent bug: the old path never recalculated `profiles.level`). 6 new tests in `syncQueue.test.ts` verify the persist-per-entry behavior directly (a simulated failure on entry 2 of 2 leaves exactly one entry queued, not two). | Guruprasath Annadurai | Phase 6 exit | Any user report of "my progress disappeared" (or duplicated) | N/A — resolved | RESOLVED |
| RISK-010 | No observability/alerting reaches any accountable person for backend, mobile, or business-critical failures (mock save failures, entitlement failures, backup job failures) | Medium | High (failures could run silently for days) | **High** | Phase 9 mandates dashboards + alert-to-owner wiring | Guruprasath Annadurai | Phase 9 exit | Any silent failure discovered after the fact via user report rather than alert | Sentry crash reporting exists for the mobile client only — not backend/business metrics | PARTIALLY COMPLETE (crash reporting only) |
| RISK-011 | No load testing has ever been performed; 10,000-user capacity is unverified in every dimension (DB connections, Edge Function concurrency, AI provider quota) | Medium (as user count grows) | High (outage or severe latency during a real growth event, e.g. a school onboarding all students at once) | **High** | **Extended 2026-08-08.** Original Phase 11 pass: k6 tooling (`docs/enterprise/load-testing/k6-baseline.js`) with the mandate's full staged profile as runnable config, plus a 20-VU smoke baseline. This pass pushed to a 60-VU, 90s-sustained read-path test — 100% success, p95=175.36ms (no degradation from 20 VUs), connections 12→17 of 60 max (28%, confirming PostgREST pools connections sub-linearly with VU count) — plus a first concurrency data point for an edge function (8 concurrent `gemini-vision` calls, all completed, correctly logged, no lock contention). **The more important finding**: confirmed via `get_organization` that **production itself is on Supabase's Free plan** — `get_connection_stats()` on production returns the exact same `max_connections: 60` as staging. This isn't a staging-only artifact; it's production's real, current ceiling. 60 total connections cannot safely serve 10,000 concurrent users under any realistic per-request connection budget once edge functions, crons, and the AI gateway's own reads share that budget — this is a **structural plan-tier limit**, not something further load testing or app-level optimization on the current plan can resolve. | Guruprasath Annadurai | Phase 11 exit | Any real usage spike (institution onboarding, marketing push) | Two clean read-path baselines (20 VU, 60 VU) plus one edge-function concurrency data point exist; the actual safe ceiling for 10,000 users depends on a Supabase plan upgrade this pass cannot authorize | PARTIALLY COMPLETE |
| RISK-012 | Subscription entitlement is not proven to be server-side-only; webhook idempotency and reconciliation are unverified | Low-Medium | High (revenue loss or incorrect free access to paid content) | **High** | **PARTIALLY RESOLVED Phase 10**: `docs/enterprise/PAYMENTS_ENTITLEMENT_AUDIT.md`. Found and fixed: (1) `subscriptions` RLS policy `sub_own` was `FOR ALL`, on paper allowing a user to insert their own fake `active` row and get `get_status`'s self-heal to grant free Pro — verified live on staging that this was NOT currently exploitable (no table-level GRANT for authenticated/anon), fixed anyway as defense-in-depth (`20260816_subscriptions_rls_hardening.sql`, SELECT-only policy) since relying solely on grants happening to also block it is fragile. (2) The client `verify_payment` call and the Razorpay `payment.captured` webhook race every real checkout; the loser hit the (correctly present) UNIQUE constraint on `razorpay_payment_id` and was mishandled as a real error — a raw 500 to the paying student on one side, a false "CRITICAL" Sentry/Slack alert on ordinary traffic on the other. Both now treat that specific Postgres 23505 as the expected benign case. Remaining open: no automated reconciliation cron exists (self-heal is reactive, only fires when the affected user themselves calls get_status); webhook signature comparisons aren't constant-time (low severity, not fixed). | Guruprasath Annadurai | Phase 10 exit | Any user report of losing/gaining access incorrectly | Reactive self-heal in get_status only — no proactive reconciliation | PARTIALLY COMPLETE |
| RISK-013 | 185 CAT exam questions (and likely other content) were inserted directly into production via ad hoc SQL with zero PR, code review, or version-control trail | Low (already happened; risk is recurrence) | Medium (an incorrect or ambiguous question could reach students with no audit trail to review against) | **Medium** | Phase 12 mandates a reviewable content workflow (draft → validation → review → approved → published) | Guruprasath Annadurai | Phase 12 exit | Any future untracked content insertion | `report-wrong-answer` reactive path exists | NOT STARTED |
| RISK-014 | Admin/teacher/parent/institution UI uses the same dark, gamified student aesthetic — no distinct professional design language exists | Low (not a functional/security risk) | Medium (credibility risk in institutional sales conversations) | **Medium** | Phase 13.5 mandates a separate administrative design system | Guruprasath Annadurai | Phase 13 exit | Any institutional prospect feedback citing this | None | NOT STARTED |
| RISK-015 | Local development toolchain versions (Node 26, Java 17) diverge from CI-pinned versions (Node 22, Java 21) | Low | Low-Medium (works-on-my-machine failures; CI is authoritative so production risk is low, but it has already caused one real incident — the hardcoded JDK path bug) | **Medium** | Document CI as the single source of truth; consider a `.nvmrc`/`.java-version` file to reduce local drift | Guruprasath Annadurai | Phase 3 | Any new "works locally, fails in CI" report | CI is authoritative and gates merges | PARTIALLY COMPLETE (CI is correct; local alignment not enforced) |
| RISK-016 | iOS has a scaffolded project (`ios/App`) but zero CI coverage, zero build verification, zero release path | Low (no current iOS release pressure) | Medium (any claim of "iOS support" would be unverified) | **Medium** | Do not claim iOS readiness until a CI job and at least one real build exist | Guruprasath Annadurai | Before any iOS release claim | Any decision to pursue an iOS release | None — Android-only is the honest current state | NOT STARTED |
| RISK-017 | Three moderate `react-router`/`react-router-dom` CVEs remain open by deliberate decision (no clean upstream version exists across all four known advisories) | Low (requires a specific navigation-injection vector not currently found in the codebase) | Medium | **Medium** | Re-check for a clean upstream release periodically; no navigate()/Link call sites currently take attacker-controlled input (verified via grep, not exhaustive) | Guruprasath Annadurai | Quarterly, or on any new react-router release | A new react-router release closing all 4 advisories simultaneously | Hold at 6.30.4 | ACCEPTED TEMPORARY RISK |
| RISK-018 | `@capacitor/assets` nested `tar` dependency has no available upstream fix | Very Low (dev-tooling-only, never runs in CI or production) | Low | **Low** | Confirmed dev-only, manual-invocation-only exposure; no untrusted input path | Guruprasath Annadurai | On next `@capacitor/assets` release | A new `@capacitor/assets` release | None needed — exposure is not reachable | ACCEPTED TEMPORARY RISK |
| RISK-019 | `get_school_leaderboard()` returned real student full names + avatar URLs for any school to any authenticated caller regardless of the caller's own school — found during Phase 1.1's individual function review | Was High (live, real, easily triggered by any signed-in user) | High (minors' PII: real names + photos) | **High (pre-fix)** | **FIXED Phase 1.1**: migration `20260806112438` adds a same-school membership check; verified live via rolled-back JWT-claim simulation confirming masked vs. real output for same-school vs. different-school callers | Guruprasath Annadurai | N/A — resolved | N/A | N/A | VERIFIED COMPLETE |
| RISK-020 | `get_school_summary()` allows any authenticated caller to probe any school's aggregate XP/rank/student-count with no membership check | Medium | Low-Medium (aggregate only, no individual PII) | **Low** | Assessed during Phase 1.1, deliberately not fixed alongside RISK-019 to keep that migration small and reviewable | Guruprasath Annadurai | Next security pass | Any report of this being used for competitive intelligence between schools | None | ACCEPTED TEMPORARY RISK |
| RISK-021 | `get_school_leaderboard()`'s name-masking logic only masks names containing a space (`"First Last"` → `"First L."`); single-word names pass through unmasked regardless of caller's school membership | Low (requires a user to have registered a single-word display name) | Low (reveals a name, not a name+photo combination, and only for users who chose a single-word name) | **Low** | Pre-existing, found incidentally while verifying RISK-019's fix. Not fixed this phase — filed as a follow-up | Guruprasath Annadurai | Next security pass | Any report of this specific masking gap | None | ACCEPTED TEMPORARY RISK |
| RISK-022 | `live_room_messages` and `live_event_participants` had `SELECT` RLS policies with `USING (true)` for the `public` role — real chat content + sender names (messages table) and participant scores (events table) were readable by anyone with zero login, found during Phase 1.2's systematic RLS review | Was High (live, zero auth barrier, real content) | High (message content; sender_name) / Medium (participant scores only) | **High (pre-fix)** | **FIXED Phase 1.2**: migration `20260806113315` requires `auth.uid() IS NOT NULL`, matching an existing pattern already used by `study_room_members` in this codebase. Verified live via a rolled-back transaction with a real synthetic row: anon saw 0 rows, authenticated saw 1. | Guruprasath Annadurai | N/A — resolved | N/A | N/A | VERIFIED COMPLETE |
| RISK-023 | `doubt_room_posts`/`doubt_room_answers` are readable by anyone with zero login, exposing `user_id` alongside post/answer content | Medium | Low-Medium (bounded — no currently-anon-reachable function resolves this `user_id` to a real name/photo, following the RISK-019 fix) | **Low-Medium** | Assessed as likely an intentional public-forum feature; not fixed this phase since restricting it would be a product behavior change, not a pure security fix, and needs a founder decision on intended UX | Guruprasath Annadurai | Next security pass, or when a name-resolution path is added anywhere that could chain with this | Any new function/table that resolves user_id → name becoming anon-reachable, or any report of unwanted public visibility | None | ACCEPTED TEMPORARY RISK |
| RISK-024 | No formal `teacher`/`parent` `app_role` enum values exist — teacher access is modeled via row ownership instead | Was Medium/unknown | N/A | **Resolved — not a gap** | **RESOLVED Phase 1.3**: checked all 7 teacher-relevant tables (`classrooms`, `classroom_members`, `classroom_assignments`, `classroom_submissions`, `teacher_assignments`, `teacher_profiles`) — every one consistently checks `auth.uid()` against a real ownership/membership column, never a client-supplied claim. Deliberate, consistent design, not a gap. | Guruprasath Annadurai | N/A — resolved | N/A | N/A | VERIFIED COMPLETE |
| RISK-025 | No parent-child/guardian relationship table exists anywhere in the schema (confirmed via `information_schema.columns` search for `%parent%`/`%child%`/`%guardian%` — only unrelated hierarchy columns found, e.g. `curriculum_topics.parent_topic_id`). `parent_reports` only self-scopes to the report-creator's own `user_id`, with no verified link to a specific student's records. | Unknown | Unknown — depends entirely on product intent | **Medium (open question, not yet a confirmed bug)** | **Needs a founder decision**, not a unilateral fix: is a parent meant to see only self-generated reports (current actual behavior, which is safe), or a specific linked child's live data (which would require building the relationship model and access control that don't currently exist)? Filed in Phase 1.3, `docs/security/ROLE_PERMISSION_MATRIX.md`. | Guruprasath Annadurai | Before any parent-facing live-data feature ships | Any parent-facing feature request beyond self-generated reports | Current behavior (self-scoped reports only) is safe as-is | NOT STARTED (decision pending) |

| RISK-026 | **Android release keystore (`android/edora-release.jks`) exists as a single file on one local machine with zero confirmed backup** — confirmed present (2,726 bytes), correctly gitignored (never in version control), found during Phase 2.1's backup architecture review. **Update 2026-08-06, full incident + remediation sequence**: (1) founder pasted the keystore password into chat twice — treated as compromised; (2) founder ran `keytool -storepasswd`/`-keypasswd` in a separate terminal tab, which completed successfully, but the new password was never recorded anywhere and `keystore.properties` was never updated — confirmed via repeated typed/copy-pasted `keytool -list` failures that the original local upload keystore (`edora-release.jks`) is locked, with no known working password; (3) checked Play Console → App integrity and confirmed Google already holds a separate, Google-managed "App signing key certificate" (Play App Signing enabled) — the locked file was only the *upload key*, not the key Google uses to sign releases, so the catastrophic scenario was ruled out; (4) generated a brand-new upload keystore (`android/edora-upload-key.jks`, alias `edora-upload`), saved its password to the founder's password manager, and exported its public certificate; (5) submitted Google Play Console's "Request upload key reset" form using the new certificate. **Update 2026-08-06 (later same night): Google approved the reset.** Confirmed via a real Play Console notification (screenshot reviewed): *"We've received a request to reset your upload key... The new upload key will become valid on Aug 8, 2026 at 11:05 AM UTC."* New upload certificate fingerprints published by Google: MD5 `0A:29:27:CD:D8:77:8B:25:F5:25:85:C7:C8:10:04:B6`, SHA1 `23:2F:AD:E7:FC:21:1D:0A:36:B3:5A:96:5A:D9:0F:73:C2:BE:DF:38` (public certificate fingerprints — not secrets, safe to record). **Approved, but not yet active** — uploads with the new key are blocked until the Aug 8, 2026 11:05 AM UTC activation time; the old (locked) key cannot be used in the meantime either, so there is a genuine ~1.5-day window where no release can be uploaded to this app, by Google's own design (a security cooldown period, not a bug). | Low (was Medium/Critical) — approved by Google, resolution is now scheduled and time-bound, not open-ended | **Recoverable, on a confirmed timeline.** Once the Aug 8, 2026 11:05 AM UTC activation passes, `edora-upload-key.jks` becomes the valid upload key for all future releases. Existing user installs and the true app signing key (Google-held) are unaffected throughout, including during the cooldown window. | **Low (was Critical)** | **Nearly complete**: (1)–(3) from the prior update remain done; (4) Google's approval confirmed live, activation scheduled for 2026-08-08 11:05 UTC; (5) still needed: back up `edora-upload-key.jks` to a second location beyond the password manager, update `android/keystore.properties` with the new key/password, and do one test signed build **after** the activation time to confirm the new key actually works end to end — attempting this before Aug 8 11:05 UTC will fail by Google's own design, not a bug to chase; (6) the original locked `edora-release.jks` can be left in place or deleted at founder's discretion — it is superseded, not a lingering risk | Guruprasath Annadurai | Low — resolution is now a confirmed, scheduled Google action, not an open-ended wait. Re-verify after 2026-08-08 11:05 UTC that a real signed build with the new key succeeds, before the next Play Store release is attempted | Any release attempt between now and 2026-08-08 11:05 UTC will fail (expected, not a new incident) — do not attempt an upload in that window | None needed — Google's process is the resolution path, already approved and scheduled | PARTIALLY COMPLETE (Google-approved, activation scheduled 2026-08-08 11:05 UTC; second backup + post-activation build verification remain) |
| RISK-027 | Storage bucket objects (user-uploaded files: avatars, study-pdfs, public-media) have zero backup — `db-backup-export` only dumps Postgres table rows, never touches `storage.objects` content | Low-Medium | Medium (individual user files permanently lost if deleted/corrupted; no aggregate financial/legal impact, but real user-facing data loss) | **Medium** | Found during Phase 2.1. No fix built this phase — would need a new backup mechanism (e.g. periodic bucket-to-bucket copy or export) | Guruprasath Annadurai | Next backup-system update | Any user report of a lost file with no ability to recover it | None | NOT STARTED |
| RISK-028 | `OAUTH_TOKEN_ENCRYPTION_KEY` has no backup and, per `SECRET_ROTATION_POLICY.md`, cannot be meaningfully "rotated" without a re-encryption migration that doesn't exist — if this key is lost (not compromised, just deleted), every currently-stored encrypted OAuth token becomes permanently undecryptable | Low | Medium (affected users would need to reconnect Google Classroom/Calendar/Gmail/Drive — inconvenient, not catastrophic, since OAuth reconnection is a normal user-facing flow) | **Medium** | Found during Phase 2.1. Recommend a secure backup of this specific key (e.g. password manager) given it's uniquely unrecoverable among this project's secrets | Guruprasath Annadurai | Next security pass | Any report of this key going missing from Supabase's secret store | None | NOT STARTED |
| RISK-029 | Local migration filenames in `supabase/migrations/` do not reliably match Supabase's actual applied-migration ledger (`list_migrations`) — confirmed concretely via this session's own 2 most recent migrations, applied via `apply_migration` and recorded under a Supabase-assigned timestamp hours different from the local filename's embedded timestamp. Found during Phase 2.5's migration-order verification; at least 10 of 175 local files show this exact pattern, with ~38 more showing no clean name match at all (likely the same phenomenon compounded with wording differences) | High (already happened repeatedly, ongoing every time `apply_migration` is used) | Medium (no data-loss risk found — live schema correctly reflects all this work — but real forward-recovery/replay-order risk if local files are ever pushed fresh via `supabase db push` in filename order) | **Medium** | Not fixed this phase — reconciling all 175 files with confidence needs individual content-diffing against live schema state, risky to rush. Recommend either always using `supabase db push` (which uses local filenames directly) instead of the `apply_migration` MCP tool going forward, or a dedicated reconciliation pass | Guruprasath Annadurai | Before any from-scratch schema rebuild is attempted | Any attempt to replay migrations from a fresh database | None | NOT STARTED |
| RISK-030 | `mock_test_attempts` only receives a row on successful completion (`completed_at` set at insert time, `MockTestPage.tsx` line 189) — there is no "started" row. A crash, connectivity drop, or app kill mid-exam leaves zero trace anywhere: not a failed row, no row at all. Found during Phase 9's observability work while investigating why "mock save failures" couldn't be monitored — the schema provides no signal to observe, so no honest check could be built | Unknown (no way to measure how often this already happens today, precisely because it leaves no trace) | High (a student could lose an entire exam attempt with no record it was ever started, and no way for support to even confirm the claim) | **High** | **RESOLVED 2026-08-08**: new `mock_test_attempt_starts` table (separate from `mock_test_attempts` so every existing consumer — postmortem, weak-topic pipeline, percentile calc — keeps assuming every row is a completed attempt, no null-checks needed anywhere else). `MockTestPage.tsx`'s `startMock()` now inserts a best-effort, fire-and-forget row the moment an attempt begins (before any answers exist), keyed by the same `attempt_key` already minted for idempotent submission (Phase 5). RLS verified live (own-insert succeeds, cross-user insert correctly rejected, via a rolled-back transaction test). `monitoring-check` now diffs `mock_test_attempt_starts` against `mock_test_attempts.attempt_key` (30-day window) and alerts on any start >6h old with no matching completion — the exact "how often does this happen" question this risk named as unanswerable is now answerable, deployed to production. | Guruprasath Annadurai | N/A — resolved | Any user report of a "lost" mock exam attempt — now independently verifiable via `mock_test_attempt_starts` | N/A — resolved | RESOLVED |
| RISK-031 | `_shared/rateLimit.ts` has diverged between functions: some already-redeployed functions carry a newer version (fails closed on a rate-limit DB error, adds a global LLM budget check), while `novo-subscription` (confirmed during Phase 9's deploy) still carries the older version (fails open, no global budget). Since Supabase Edge Functions bundle `_shared/*.ts` per-function at deploy time rather than sharing a live module, this drift can persist indefinitely until each function is individually redeployed | Medium (silent, accumulates over time as some functions get redeployed for unrelated reasons and others don't) | Low-Medium (fail-open on a rate-limit check is the less safe direction, but only manifests during an actual rate-limit-table outage, not routine operation) | **Low-Medium** | Not fixed this phase — deliberately excluded from Phase 9's `novo-subscription` deploy to keep that change surgical (see `docs/enterprise/phase-9/PHASE_9_OBSERVABILITY_BOOTSTRAP.md`). Recommend a dedicated pass auditing which functions still carry stale shared-file copies and redeploying them | Guruprasath Annadurai | Next dedicated maintenance pass | Any other function found to still have functionally-outdated shared code at its next deploy | None | NOT STARTED |
| RISK-032 | `pyq_content` (the live exam-question table for CAT/BOARDS/UPSC/JEE/NEET) has `is_reviewed`, `flagged_for_review`, and `is_active` columns clearly intended to gate content quality, but **no application query anywhere filters on any of them**, and until this phase, RLS didn't enforce them either. Confirmed via full app-code audit (every `.from('pyq_content')` call site checked) and live data query: 233 of 555 rows (42%) have `is_reviewed=false` — and this isn't scattered noise, it's **100% of all CAT, BOARDS, and UPSC content** (JEE/NEET are 100% reviewed). Found during Phase 12's content-governance work | High (already true today — unreviewed content has been served to every student studying CAT/BOARDS/UPSC since it was added, indistinguishably from reviewed content) | Medium-High (academic accuracy risk for 3 entire exam categories; no evidence of actual factual errors found, but the review gate that should have caught them was never functioning) | **High** | **Extended 2026-08-08**: found that a real, working, more sophisticated AI review pipeline for exactly this backlog already exists in production (`pyq-content-audit`, Nemotron-primary/Gemini-fallback, nightly cron) — but was silently failing 100% of the time (`cron_health` shows the 2026-08-08 nightly run saw 20/233 backlog candidates and got 20/20 `inconclusive`, consistent with a missing/invalid provider API key). This pipeline was invisible to `monitoring-check`, which had 5 alert checks but none reading `cron_health` — fixed by adding a 6th check that alerts on any non-success cron_health status, deployed to production. Also discovered and pulled back into git a whole different, better implementation of `pyq-content-audit` than the stale draft previously committed there (see `docs/enterprise/RISK032_CONTENT_REVIEW_WORKFLOW.md`). **`is_reviewed` enforcement still deliberately NOT applied** and **no content was manually marked reviewed this pass** — both remain the founder's decision, unchanged from Phase 12's framing. What changed is that fixing the provider-key issue (likely a small, mechanical fix) would let the existing real pipeline clear the backlog itself, which is a materially smaller ask than "commission a manual review of 233 questions." **Extended 2026-08-18**: ran `run_audit` live against production and got the real root cause, not the guessed one — it is NOT (only) the provider key. Two separate, confirmed, non-code blockers: (1) `NVIDIA_API_KEY` was never configured at all (`hasNvidiaKey: false`, confirmed via a live probe) — Nemotron has never actually run once; (2) the Gemini fallback is independently blocked by depleted prepaid billing credits on the Google AI Studio project — confirmed via the real HTTP 429 `RESOURCE_EXHAUSTED` response ("Your prepayment credits are depleted..."), not a code or model-ID issue. Separately, discovered and fixed a real, live, more severe bug found via this investigation: `gemini-1.5-flash`/`gemini-2.0-flash` (30 functions) and `llama-3.3-70b-versatile`/`llama-3.1-8b-instant` (11 functions, including gemini-chat's PRIMARY chat model, not just a fallback) had all been decommissioned by their providers — confirmed via each provider's live model-list endpoint, not assumption — and fixed across every call site, committed (`b6446da`). Added an 8th `monitoring-check` alert that makes one real completion call per model this app depends on so this exact failure class is caught automatically going forward. The 233-row content backlog itself remains unchanged — it cannot be cleared without founder action on two accounts this session has no authority over: add `NVIDIA_API_KEY` as a secret, and add billing/credits at ai.studio/projects. | Guruprasath Annadurai | Before any further content is added to these 3 exams without review | Any user-reported factual error in CAT/BOARDS/UPSC content — currently nothing would distinguish "reviewed content with an error" from "this whole category was never reviewed" | Add `NVIDIA_API_KEY` as a secret; add Gemini billing/credits at ai.studio/projects; or accept the current unenforced state; or commission manual review — the founder's decision either way | PARTIALLY COMPLETE (tooling fixed and a separate live model-decommission bug found+fixed across 41 functions as a side effect; content backlog itself blocked on two real account/billing issues outside this session's authority) |
| RISK-033 | Local migration files in `supabase/migrations/` could not rebuild production's schema from scratch — confirmed concretely, not theoretically, during Gate 2 (4.1.0 staging environment) while bootstrapping a genuinely empty database. Initial discovery: at least 3 tables that exist on production with real data (`classrooms`, `classroom_members`, `mains_answer_submissions`) were never created by any local migration file, only referenced by later files' foreign keys. This is a stronger, concrete instance of the drift RISK-029 already flagged (filename-vs-ledger mismatch); this was worse — entire table definitions were missing, not just naming | High (was already true, and would have been re-discovered by anyone attempting a from-scratch rebuild) | High (directly undermined the RPO/RTO recovery claims made in Phase 2's backup/restore work) | **High** | **Substantially resolved.** Rather than writing a `supabase db diff` baseline (the originally planned fix, blocked by Docker Desktop disk exhaustion making the local shadow-DB tooling unusable), the full local migration set (178 files) was replayed end to end against `edora-staging` via direct `execute_sql` calls through the Supabase MCP, with `supabase_migrations.schema_migrations` manually kept in sync using each file's real local version (not `apply_migration`'s auto-generated timestamp, which would have reintroduced RISK-029's drift). This is a more thorough validation than a diff baseline would have provided: every one of ~30 real historical bugs (forward references, invalid Postgres syntax, ordering/version collisions, function-overload ambiguity, pgvector schema-relocation fallout, and outright application bugs) was found and fixed in place, each with its own reviewed branch and commit (`enterprise/426`–`enterprise/435`). Verified: `schema_migrations` count on staging = 178 = exact local file count | Guruprasath Annadurai | Complete for Gate 2. Re-verify if/when Docker Desktop is healthy again, by independently confirming `supabase db push` against a fresh database produces the same 178-migration result | Any attempt to rebuild the schema from scratch (staging bootstrap, disaster recovery drill, new environment) | Production itself was never affected — this was always a local-files-only gap, live schema was fine throughout | RESOLVED (178/178 migrations verified to replay cleanly from empty; see Gate 2 completion report for full bug list) |
| RISK-034 | Full individual triage completed 2026-08-08 — see `docs/enterprise/RISK034_SECURITY_DEFINER_TRIAGE.md`. The original "165 findings" baseline was captured against `edora-staging`, which turned out to be stale/drifted relative to production (the same undocumented-drift pattern as RISK-029/033): production's real baseline was only 50 findings, 0 ERROR (the 7 view findings and most search_path findings had already been fixed out-of-band, just never committed to a migration). Found and fixed 2 genuine live bugs during individual review: `buddy_checkin()` never verified the caller was a party to the pair (authorization bypass enabling arbitrary bonus-XP grants to uninvolved users), and `set_rag_cache`/`get_rag_cache` were still `authenticated`-callable despite a prior migration's incorrect assumption that only anon needed revoking — any signed-in user could poison the shared AI-response cache served to other students. 9 functions had unnecessary `anon`/`authenticated`/`PUBLIC` grants revoked (verified via full-repo grep that none are called directly by client code) after confirming via `pg_policies` which helper functions (`is_institution_admin`/`is_institution_member`/`is_in_study_group`) **cannot** have their grants revoked without breaking RLS for every authenticated user. The 7 views' `security_invoker=on` fix was formally committed to a migration for the first time (`20260808_risk034_security_definer_triage.sql`) and applied to both production (idempotent) and staging (fixed real drift). | Was Medium | Was Medium-High | **Low (residual)** | **Substantially resolved.** Production findings reduced 50→30 (28 of which are the app's legitimate, individually-reviewed RPC surface — each checks `auth.uid()` before any sensitive write/read, or intentionally serves aggregate/public content). 1 residual finding accepted with documented reasoning (institution/study-group membership enumeration — fixing requires breaking real RLS enforcement, not a unilateral call to make). | Guruprasath Annadurai | N/A — resolved for all objects reviewed; new SECURITY DEFINER objects added later need the same discipline | Any new SECURITY DEFINER function/view added without documented reasoning for the elevated privilege | N/A — resolved | VERIFIED COMPLETE (for all objects flagged as of this pass) |

## Review cadence

No recurring review cadence currently exists (this is itself Phase 14 work). Until then, this register will be re-read and updated at the start of every phase per the mandate's mandatory working method.
